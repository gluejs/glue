name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: make glue
    - name: Archive
      run: make dist
    - uses: actions/upload-artifact@v1
      with:
        name: build-artifacts
        path: dist/
